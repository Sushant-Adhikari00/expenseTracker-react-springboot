databaseChangeLog:
  - changeSet:
      id: 002-create-incomes-table
      author: expense-tracker
      changes:
        - createTable:
            tableName: incomes
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false

              - column:
                  name: user_id
                  type: BIGINT
                  constraints:
                    nullable: false
                    foreignKeyName: fk_incomes_user
                    references: users(id)
                    deleteCascade: true

              - column:
                  name: source
                  type: VARCHAR(100)
                  constraints:
                    nullable: false

              - column:
                  name: amount
                  type: DECIMAL(15, 2)
                  constraints:
                    nullable: false

              - column:
                  name: date
                  type: DATE
                  constraints:
                    nullable: false

              - column:
                  name: category
                  type: VARCHAR(50)
                  constraints:
                    nullable: false

              - column:
                  name: note
                  type: VARCHAR(500)

              - column:
                  name: created_at
                  type: TIMESTAMP
                  defaultValueComputed: CURRENT_TIMESTAMP
                  constraints:
                    nullable: false

        - createIndex:
            tableName: incomes
            indexName: idx_incomes_user_id
            columns:
              - column:
                  name: user_id

        - createIndex:
            tableName: incomes
            indexName: idx_incomes_date
            columns:
              - column:
                  name: date